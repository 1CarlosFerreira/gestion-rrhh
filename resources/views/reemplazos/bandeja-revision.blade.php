<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Reemplazos · Gestión de Personas</h2></x-slot><div class="py-10"><div class="mx-auto max-w-7xl space-y-4 px-4">@if(session('status'))<div class="rounded bg-green-50 p-3 text-green-800">{{ session('status') }}</div>@endif<div class="overflow-hidden rounded-xl bg-white shadow"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-3">Código</th><th>Unidad</th><th>Funcionario / reemplazante</th><th>Períodos</th><th>Estado</th><th>Envío</th><th></th></tr></thead><tbody>@forelse($tramites as $tramite)<tr class="border-t {{ $tramite->estadoTramite->codigo === 'ENVIADA_GESTION_PERSONAS' ? 'bg-amber-50' : '' }}"><td class="p-3 font-mono">{{ $tramite->codigo }}</td><td>{{ $tramite->unidadOrganizacional->nombre }}</td><td>{{ $tramite->reemplazo->funcionario->nombre_completo }}<br>{{ $tramite->reemplazo->reemplazante->nombre_completo }}</td><td>{{ $tramite->reemplazo->fecha_funcionario_desde->format('d/m/Y') }}–{{ $tramite->reemplazo->fecha_funcionario_hasta->format('d/m/Y') }}<br>{{ $tramite->reemplazo->fecha_reemplazante_desde->format('d/m/Y') }}–{{ $tramite->reemplazo->fecha_reemplazante_hasta->format('d/m/Y') }}</td><td>{{ $tramite->estadoTramite->nombre }}</td><td>{{ $tramite->submitted_at?->format('d/m/Y H:i') }}</td><td><a class="font-semibold text-indigo-700" href="{{ route('gestion-personas.reemplazos.show',$tramite) }}">Ver/Revisar</a></td></tr>@empty<tr><td colspan="7" class="p-6 text-center text-slate-500">No hay solicitudes pendientes en su alcance.</td></tr>@endforelse</tbody></table></div>{{ $tramites->links() }}</div></div></x-app-layout>
