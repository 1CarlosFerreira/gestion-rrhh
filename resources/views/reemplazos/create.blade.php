<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Nueva Solicitud de Reemplazo</h2></x-slot><div class="py-10"><form method="POST" action="{{ route('reemplazos.store') }}" class="mx-auto max-w-3xl space-y-4 rounded-lg bg-white p-6 shadow-sm">@csrf<p>Seleccione la unidad para crear un borrador. El tipo de trámite y estado se resuelven automáticamente.</p><select name="unidad_servicio_id" required class="w-full rounded border-gray-300"><option value="">Unidad/Servicio</option>@foreach($unidades as $unidad)<option value="{{ $unidad->id }}">{{ $unidad->nombre }}</option>@endforeach</select><x-input-error :messages="$errors->all()"/><x-primary-button>Crear borrador</x-primary-button></form></div></x-app-layout>
