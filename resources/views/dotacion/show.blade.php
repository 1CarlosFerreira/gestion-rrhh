<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-slate-800">Hoja de vida del funcionario</h2></x-slot>
    <div class="py-10"><div class="mx-auto max-w-7xl space-y-6 px-4 sm:px-6 lg:px-8">
        <a href="{{ route('dotacion.index', request()->only(['unidad_id', 'q', 'page'])) }}" class="inline-flex text-sm font-semibold text-indigo-700 hover:text-indigo-900">← Volver a Dotación</a>
        <p class="-mt-4 text-slate-600">Consulta sus vínculos y participación en los trámites gestionados por tu unidad.</p>
        <section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col gap-5 sm:flex-row sm:items-start"><x-avatar-initials :name="$persona->nombre_completo" size="h-16 w-16" class="text-lg"/><div class="min-w-0 flex-1"><div class="flex flex-wrap justify-between gap-3"><div><h1 class="text-2xl font-semibold text-slate-900">{{ $persona->nombre_completo }}</h1><p class="mt-1 text-slate-600">{{ \App\Support\Rut\Rut::format($persona->rut) }}</p></div><span class="h-fit rounded-full px-2.5 py-1 text-sm font-medium {{ $persona->active ? 'bg-emerald-100 text-emerald-800' : 'bg-slate-100 text-slate-700' }}">{{ $persona->active ? 'Activa' : 'Inactiva' }}</span></div><dl class="mt-5 grid gap-4 text-sm sm:grid-cols-2 lg:grid-cols-4"><div><dt class="text-slate-500">Unidad actual</dt><dd class="mt-1 font-medium">{{ $actual?->unidad?->nombre ?? 'No informado' }}</dd></div><div><dt class="text-slate-500">Estamento</dt><dd class="mt-1">{{ $actual?->estamento?->nombre ?? 'No informado' }}</dd></div><div><dt class="text-slate-500">Profesión</dt><dd class="mt-1">{{ $actual?->profesion?->nombre ?? 'No informado' }}</dd></div><div><dt class="text-slate-500">Cargo o función</dt><dd class="mt-1">{{ $actual?->cargo_texto ?: 'No informado' }}</dd></div></dl>@if($actual)<p class="mt-4 text-sm text-slate-600">Vínculo vigente desde {{ $actual->start_date?->translatedFormat('d M. Y') ?? 'fecha no informada' }}</p>@endif</div></div></section>
        <section class="grid gap-3 sm:grid-cols-3">@foreach([['Vínculos registrados', $vinculos->count(), '#vinculos'], ['Reemplazos relacionados', $reemplazos->total(), '#reemplazos'], ['Participaciones en Horas Extraordinarias', $horasExtra->total(), '#horas-extra']] as [$label, $count, $target])<a href="{{ $target }}" class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:border-indigo-200"><p class="text-sm text-slate-600">{{ $label }}</p><p class="mt-1 text-2xl font-semibold text-slate-900">{{ $count }}</p></a>@endforeach</section>
        <section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-semibold text-slate-900">Vínculo actual</h2>@if($actual)<dl class="mt-4 grid gap-4 text-sm sm:grid-cols-2 lg:grid-cols-3"><div><dt class="text-slate-500">Unidad o servicio</dt><dd>{{ $actual->unidad->nombre }}</dd></div><div><dt class="text-slate-500">Estamento</dt><dd>{{ $actual->estamento?->nombre ?? 'No informado' }}</dd></div><div><dt class="text-slate-500">Profesión</dt><dd>{{ $actual->profesion?->nombre ?? 'No informado' }}</dd></div><div><dt class="text-slate-500">Cargo</dt><dd>{{ $actual->cargo_texto ?: 'No informado' }}</dd></div><div><dt class="text-slate-500">Inicio</dt><dd>{{ $actual->start_date?->translatedFormat('d M. Y') ?? 'No informado' }}</dd></div><div><span class="inline-flex rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-medium text-emerald-800">Vigente</span></div></dl>@else<p class="mt-3 text-sm text-slate-600">No registra un vínculo vigente en las unidades que tienes autorizadas.</p>@endif</section>
        <section id="vinculos" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-semibold text-slate-900">Historial de vínculos</h2><div class="mt-4 space-y-3">@forelse($vinculos as $v)<article class="rounded-lg border border-slate-200 p-4"><div class="grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-4"><p><span class="block text-slate-500">Unidad</span>{{ $v->unidad->nombre }}</p><p><span class="block text-slate-500">Estamento / profesión</span>{{ $v->estamento?->nombre ?? 'No informado' }} · {{ $v->profesion?->nombre ?? 'No informado' }}</p><p><span class="block text-slate-500">Cargo</span>{{ $v->cargo_texto ?: 'No informado' }}</p><p><span class="block text-slate-500">Período</span>{{ $v->start_date?->translatedFormat('d M. Y') ?? 'No informado' }} · {{ $v->end_date?->translatedFormat('d M. Y') ?? 'Sin fecha de término' }}</p></div><span class="mt-3 inline-flex rounded-full px-2.5 py-1 text-xs font-medium {{ $v->id === $actual?->id ? 'bg-emerald-100 text-emerald-800' : 'bg-slate-100 text-slate-700' }}">{{ $v->id === $actual?->id ? 'Vigente' : ucfirst(strtolower($v->status)) }}</span></article>@empty<p class="text-sm text-slate-600">No registra vínculos anteriores.</p>@endforelse</div></section>
        <section id="reemplazos" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-semibold text-slate-900">Reemplazos</h2><div class="mt-4 space-y-3">@forelse($reemplazos as $r) @php($state = $r->tramite->estadoTramite->codigo) @php($editable = in_array($state, ['BORRADOR', 'DEVUELTA_CORRECCION'], true)) @php($url = $editable ? route('reemplazos.edit', $r->tramite) : route('tramites.show', $r->tramite)) @php($action = $state === 'BORRADOR' ? 'Continuar' : ($state === 'DEVUELTA_CORRECCION' ? 'Corregir' : ($state === 'FORMALIZADA' ? 'Ver resultado' : 'Ver detalle'))) <article class="rounded-lg border border-slate-200 p-4"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-semibold text-slate-900">{{ $r->tramite->codigo }}</p><p class="mt-1 text-sm text-slate-600">{{ $r->funcionario_id === $persona->id ? 'Funcionario reemplazado' : 'Reemplazante propuesto' }} · {{ $r->tramite->unidadServicio->nombre }}</p><p class="mt-1 text-sm text-slate-600">{{ $r->fecha_inicio?->translatedFormat('d M. Y') ?? 'Período no informado' }}{{ $r->fecha_termino ? ' – '.$r->fecha_termino->translatedFormat('d M. Y') : '' }}</p></div><x-status-badge :estado="$r->tramite->estadoTramite" /></div><div class="mt-3 flex justify-between text-sm"><span class="text-slate-500">Actualizado {{ $r->tramite->updated_at->format('d-m-Y H:i') }}</span><a href="{{ $url }}" class="font-semibold text-indigo-700">{{ $action }} →</a></div></article>@empty<p class="text-sm text-slate-600">No registra participaciones en solicitudes de reemplazo.</p>@endforelse</div>{{ $reemplazos->links() }}</section>
        <section id="horas-extra" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-semibold text-slate-900">Horas Extraordinarias</h2><div class="mt-4 space-y-3">@forelse($horasExtra as $h)<article class="rounded-lg border border-slate-200 p-4"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-semibold">{{ $h->tramiteHorasExtra->tramite->codigo }}</p><p class="mt-1 text-sm text-slate-600">Horas Extraordinarias · {{ $h->tramiteHorasExtra->period_start?->translatedFormat('F Y') ?? 'Período no informado' }}</p><p class="text-sm text-slate-600">{{ $h->tramiteHorasExtra->tramite->unidadServicio->nombre }}</p>@if($h->total_minutes !== null)<p class="mt-1 text-sm text-slate-600">{{ \App\Support\Documentos\DuracionMinutos::asText($h->daytime_minutes ?? 0) }} diurnas · {{ \App\Support\Documentos\DuracionMinutos::asText($h->night_festive_minutes ?? 0) }} nocturnas/festivas</p>@else<p class="mt-1 text-sm text-slate-500">Horas finales aún no informadas.</p>@endif</div><x-status-badge :estado="$h->tramiteHorasExtra->tramite->estadoTramite" /></div><a href="{{ route('tramites.show', $h->tramiteHorasExtra->tramite) }}" class="mt-3 inline-block text-sm font-semibold text-indigo-700">Ver detalle →</a></article>@empty<p class="text-sm text-slate-600">No registra participaciones en solicitudes de horas extraordinarias.</p>@endforelse</div>{{ $horasExtra->links() }}</section>
    </div></div>
</x-app-layout>
