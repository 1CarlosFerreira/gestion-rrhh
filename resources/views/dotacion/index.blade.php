<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Dotación por Unidad/Servicio</h2></x-slot>
    <div class="py-10"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8">
        <form method="GET" class="grid gap-3 rounded-lg bg-white p-5 shadow-sm md:grid-cols-3"><select name="unidad_id" class="rounded border-gray-300">@foreach($unidades as $item)<option value="{{ $item->id }}" @selected($unidad?->id === $item->id)>{{ $item->nombre }}</option>@endforeach</select><x-text-input name="q" :value="request('q')" placeholder="RUT, nombre o apellido"/><x-primary-button>Consultar</x-primary-button></form>
        @if($vinculos)<div class="overflow-x-auto rounded-lg bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-gray-50"><tr><th class="p-3">Persona</th><th class="p-3">RUT</th><th class="p-3">Estamento</th><th class="p-3">Profesión</th><th class="p-3">Cargo</th><th class="p-3">Estado</th></tr></thead><tbody>@forelse($vinculos as $vinculo)<tr class="border-t"><td class="p-3">{{ $vinculo->persona->nombre_completo }}</td><td class="p-3">{{ \App\Support\Rut\Rut::format($vinculo->persona->rut) }}</td><td class="p-3">{{ $vinculo->estamento?->nombre ?? '—' }}</td><td class="p-3">{{ $vinculo->profesion?->nombre ?? '—' }}</td><td class="p-3">{{ $vinculo->cargo_texto ?? '—' }}</td><td class="p-3">{{ $vinculo->status }}</td></tr>@empty<tr><td colspan="6" class="p-5 text-center text-gray-500">Sin dotación operativa.</td></tr>@endforelse</tbody></table></div>{{ $vinculos->links() }}@else<div class="rounded bg-white p-5 text-gray-500">No hay unidades habilitadas.</div>@endif
    </div></div>
</x-app-layout>
