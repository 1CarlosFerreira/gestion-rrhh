<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Editar {{ $tramite->codigo }}</h2></x-slot><div class="py-10"><div class="mx-auto max-w-4xl space-y-4">@if(session('status'))<div class="rounded bg-green-50 p-3 text-green-800">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('horas-extra.update', $tramite) }}" class="space-y-5 rounded-lg bg-white p-6 shadow-sm">@include('horas-extra._form')</form><form method="POST" action="{{ route('horas-extra.transition', [$tramite, 'ENVIAR_A_GESTION_PERSONAS']) }}">@csrf<x-primary-button>4. Solicitar planillas a Gestión de Personas</x-primary-button></form></div></div></x-app-layout>
