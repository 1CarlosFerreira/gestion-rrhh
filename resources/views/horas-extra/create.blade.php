<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Nueva solicitud de Horas Extraordinarias</h2></x-slot><div class="py-10"><div class="mx-auto max-w-4xl">@if(session('status'))<div class="mb-4 rounded bg-green-50 p-3 text-green-800">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('horas-extra.store') }}" class="space-y-5 rounded-lg bg-white p-6 shadow-sm">@include('horas-extra._form')</form></div></div></x-app-layout>
