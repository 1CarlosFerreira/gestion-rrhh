<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Nuevo trámite raíz</h2></x-slot><div class="py-10"><form method="POST" action="{{ route('tramites.store') }}" class="mx-auto grid max-w-3xl gap-4 rounded-lg bg-white p-6 shadow-sm">@csrf<p class="text-sm text-gray-600">Esta pantalla crea solo el registro transversal. Los formularios específicos se incorporarán en sus fases correspondientes.</p><select name="tipo_tramite_id" required class="rounded border-gray-300"><option value="">Tipo de trámite</option>@foreach($tipos as $item)<option value="{{ $item->id }}">{{ $item->nombre }}</option>@endforeach</select><select name="unidad_servicio_id" required class="rounded border-gray-300"><option value="">Unidad/Servicio</option>@foreach($unidades as $item)<option value="{{ $item->id }}">{{ $item->nombre }}</option>@endforeach</select><x-input-error :messages="$errors->all()"/><x-primary-button>Crear borrador</x-primary-button></form></div></x-app-layout>
