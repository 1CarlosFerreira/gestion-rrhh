<form method="GET" class="grid gap-3 rounded-xl border border-slate-200 bg-white p-5 shadow-sm md:grid-cols-2 lg:grid-cols-4">
    <select name="tipo_tramite_id" aria-label="Tipo de trámite" class="rounded border-gray-300"><option value="">Todos los tipos</option>@foreach($tipos as $item)<option value="{{ $item->id }}" @selected((string) request('tipo_tramite_id') === (string) $item->id)>{{ $item->nombre }}</option>@endforeach</select>
    <select name="estado_tramite_id" aria-label="Estado" class="rounded border-gray-300"><option value="">Todos los estados</option>@foreach($estados as $item)<option value="{{ $item->id }}" @selected((string) request('estado_tramite_id') === (string) $item->id)>{{ $item->nombre }} ({{ $item->tipoTramite?->nombre }})</option>@endforeach</select>
    @if($unidades->count() > 1)<select name="unidad_servicio_id" aria-label="Unidad o servicio" class="rounded border-gray-300"><option value="">Todas las unidades</option>@foreach($unidades as $item)<option value="{{ $item->id }}" @selected((string) request('unidad_servicio_id') === (string) $item->id)>{{ $item->nombre }}</option>@endforeach</select>@elseif($unidades->first())<div class="rounded border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-600">{{ $unidades->first()->nombre }}</div>@endif
    @if($withSearch ?? false)<x-text-input name="codigo" :value="request('codigo')" placeholder="Código, RUT o funcionario" aria-label="Código, RUT o funcionario"/><label class="text-sm text-slate-600">Desde<x-text-input type="date" name="desde" :value="request('desde')" class="mt-1 w-full"/></label><label class="text-sm text-slate-600">Hasta<x-text-input type="date" name="hasta" :value="request('hasta')" class="mt-1 w-full"/></label>@endif
    <div class="flex items-end gap-2"><x-primary-button class="w-auto">Filtrar</x-primary-button><a href="{{ route('tramites.index') }}" class="rounded-md px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-100">Limpiar filtros</a></div>
</form>
