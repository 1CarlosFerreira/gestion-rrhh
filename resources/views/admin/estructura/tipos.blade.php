<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Tipos organizacionales</h2></x-slot><div class="py-10"><div class="mx-auto max-w-5xl space-y-4 px-4">@if(session('status'))<div class="rounded bg-green-50 p-3">{{ session('status') }}</div>@endif
<form method="POST" action="{{ route('admin.tipos-organizacionales.store') }}" class="grid gap-2 rounded bg-white p-4 shadow sm:grid-cols-5">@csrf<x-text-input name="codigo" placeholder="Código"/><x-text-input name="nombre" placeholder="Nombre"/><x-text-input name="descripcion" placeholder="Descripción"/><x-text-input type="number" min="0" name="orden" value="0"/><label><input type="checkbox" name="activo" value="1" checked> Activo</label><x-primary-button>Crear tipo</x-primary-button></form>
@foreach($tipos as $tipo)<form method="POST" action="{{ route('admin.tipos-organizacionales.update', $tipo) }}" class="grid gap-2 rounded bg-white p-4 shadow sm:grid-cols-6">@csrf @method('PUT')<x-text-input name="codigo" value="{{ $tipo->codigo }}"/><x-text-input name="nombre" value="{{ $tipo->nombre }}"/><x-text-input name="descripcion" value="{{ $tipo->descripcion }}"/><x-text-input type="number" min="0" name="orden" value="{{ $tipo->orden }}"/><label><input type="checkbox" name="activo" value="1" @checked($tipo->activo)> Activo</label><x-primary-button>Guardar</x-primary-button></form>@endforeach
<a href="{{ route('admin.estructura.index') }}">Volver a la estructura</a></div></div></x-app-layout>
