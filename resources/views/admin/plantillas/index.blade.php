<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Plantillas documentales</h2></x-slot><div class="py-10"><div class="mx-auto max-w-6xl overflow-hidden rounded-lg bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-gray-50"><tr><th class="p-3">Código</th><th>Trámite</th><th>Documento</th><th>Versión</th><th>Estado</th><th>Vigencia</th></tr></thead><tbody>@forelse($plantillas as $plantilla)<tr class="border-t"><td class="p-3 font-mono">{{ $plantilla->codigo }}</td><td>{{ $plantilla->tipoTramite->nombre }}</td><td>{{ $plantilla->tipoDocumento->nombre }}</td><td>{{ $plantilla->version }}</td><td>{{ $plantilla->active ? 'Activa' : 'Inactiva' }}</td><td>{{ $plantilla->valid_from?->format('d-m-Y') ?? '—' }} / {{ $plantilla->valid_to?->format('d-m-Y') ?? '—' }}</td></tr>@empty<tr><td colspan="6" class="p-4">No hay plantillas registradas.</td></tr>@endforelse</tbody></table></div></div></x-app-layout>
